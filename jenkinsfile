pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                script{
                    def datas = readYaml file: "${WORKSPACE}/projects.yaml"
                    def f = load "${WORKSPACE}/folder.groovy"
                    f(datas)
                }
            }
        }
    }
}