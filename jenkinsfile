pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                script{
                    def d = load "${WORKSPACE}/folder.groovy"
                    d()
                }
            }
        }
    }
}