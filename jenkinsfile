pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                script{
                    def datas = readYaml file: "${WORKSPACE}/projects.yaml"
                    createFolders("${WORKSPACE}/folder.groovy", datas)
                }
            }
        }
    }
}