pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                script{
                    def datas = readYaml file: "${WORKSPACE}/projects.yaml"
                    println datas
                    def createFolders = load "${WORKSPACE}/folder.groovy"
                    createFolders.d(datas)
                }
            }
        }
    }
}